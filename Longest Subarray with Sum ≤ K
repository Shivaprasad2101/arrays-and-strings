int left=0,right=0;
int sum=0;
int maxlen=0;

for(right=0;right<nums.size();right++){
	sum+=nums[right];
	
	while(sum>k){
		sum-=nums[left++];
	}
	
	maxlen=max(maxlen,right-left+1);
}

return maxlen;
